image: node

before_script:
  - npm install

cache:
  paths:
    - node_modules/
    - .next/cache/

pages:
  script:
    - npm run-script build && npm run-script export
    - mv out/* public
  after_script:
    - rm -rf out
  artifacts:
    paths:
      - public
  only:
    - main